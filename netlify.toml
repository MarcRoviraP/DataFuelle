[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Programamos el disparador (trigger) para las 00:00 y 12:00 (UTC)
[functions."sync-miteco-cron"]
  schedule = "0 0,12 * * *"
 