[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Programamos la sincronización para las 00:00 y 12:00 (UTC)
[functions."sync-miteco-background"]
  schedule = "0 0,12 * * *"
 